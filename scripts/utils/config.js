// Fake Data for prior development till backend is not ready . We use the below URL from git as fake data.
// Never use URL in code 
// Configurations are never part of code
export const URL = 'https://yashdxr.github.io/Pizza-Shop/server.json' ;
